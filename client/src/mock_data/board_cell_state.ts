/**
 * The four mutually exclusive visual states a board cell can render as.
 * Shared vocabulary between the mock board data and BoardCellView.
 */
export type BoardCellState = 'empty' | 'filled' | 'penalty' | 'active';
